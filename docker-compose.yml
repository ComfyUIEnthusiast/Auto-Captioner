services:
  autotagger:
    build: .
    container_name: autotagger
    ports:
      - "5000:5000"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - OPENAI_URL=http://192.168.1.207:8000/v1
